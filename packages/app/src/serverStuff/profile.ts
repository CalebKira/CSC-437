

export interface Profile {
    userid: string;
    /* specified id to make every user unique */
    pseudonym: string | string;
    /* the name that the user wants to go by */
    email: string;
    
}